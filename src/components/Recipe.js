import React from 'react'

const Recipe = ({ hitts }) => {
  console.log(hitts)
  // const {recepie:}
  return (
    <div>
      {/* <div>{title}</div>
      <img src={image} alt=''></img>
      <div>{calories}</div>
      <div>{calories}</div>
      {digest.map((item) => (
        <div>
          {item.label}: {item.total}
        </div>
      ))} */}
    </div>
  )
}

export default Recipe
